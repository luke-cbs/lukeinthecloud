---
export interface Props {
    logo: string;
}

const { logo } = Astro.props;
---

<img class="logo" src={`/public/logos/${logo}.svg`} alt="" />

<style>
    .logo {
        opacity: .5;
        width: 5rem;
        height: 5rem;
    }
</style>
